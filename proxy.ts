import{NextResponse}from'next/server';import type{NextRequest}from'next/server';export function proxy(r:NextRequest){if(!r.nextUrl.pathname.startsWith('/admin')||r.nextUrl.pathname==='/admin/login')return NextResponse.next();if(!r.cookies.get('nella_session')?.value)return NextResponse.redirect(new URL('/admin/login',r.url));return NextResponse.next()}export const config={matcher:['/admin/:path*']}
