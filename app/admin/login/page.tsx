"use client";import{useState}from'react';import{useRouter}from'next/navigation';export default function Login(){const[e,setE]=useState(''),[p,setP]=useState(''),[err,setErr]=useState('');const r=useRouter();async function go(x:any){x.preventDefault();const q=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:e,password:p})});if(q.ok)r.replace('/admin');else setErr((await q.json()).error)}return <main className="authPage"><form className="authCard" onSubmit={go}><span className="eyebrow">NELLA PREMIUM</span><h1>Вход в админку</h1><input type="email" placeholder="Email" value={e} onChange={x=>setE(x.target.value)} required/><input type="password" placeholder="Пароль" value={p} onChange={x=>setP(x.target.value)} required/><button className="darkButton fullButton">Войти</button>{err&&<p className="error">{err}</p>}</form></main>}
