'use client';
import {useEffect,useState} from 'react';import {Product} from '@/lib/products';import {ProductCard} from '@/components/ProductCard';
const cats=[['Все',''],['Платья','dresses'],['Рубашки','shirts'],['Брюки','pants'],['Костюмы','suits'],['Футболки','tshirts'],['Верхняя одежда','outerwear']];
export default function Catalog(){const[p,setP]=useState<Product[]>([]),[cat,setCat]=useState(''),[sort,setSort]=useState('new'),[loading,setLoading]=useState(true);useEffect(()=>{setLoading(true);const q=new URLSearchParams();if(cat)q.set('category',cat);q.set('sort',sort);fetch('/api/products?'+q).then(r=>r.json()).then(setP).finally(()=>setLoading(false))},[cat,sort]);return <main className="catalogPage"><div className="catalogTitle"><span className="eyebrow">КАТАЛОГ</span><h1>Одежда Nella Premium</h1><p>Товары загружаются из PostgreSQL через backend API.</p></div><div className="catalogToolbar"><div className="filters">{cats.map(([n,s])=><button className={cat===s?'active':''} onClick={()=>setCat(s)} key={n}>{n}</button>)}</div><select value={sort} onChange={e=>setSort(e.target.value)}><option value="new">Сначала новые</option><option value="price">Сначала дешевле</option><option value="priceDesc">Сначала дороже</option></select></div>{loading?<p>Загрузка...</p>:<div className="catalogGrid">{p.map(x=><ProductCard key={x.id} product={x}/>)}</div>}</main>}
